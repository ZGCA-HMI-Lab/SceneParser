from setuptools import find_packages, setup


setup(
    name="sceneparser",
    version="0.1.0",
    description="SceneParser training and evaluation code.",
    packages=find_packages(),
    python_requires=">=3.10",
)
